import React from 'react'
import {Field, reduxForm} from 'redux-form'
import {Form, Button,FieldGroup} from 'react-bootstrap';
import {withRouter} from 'react-router'
// import validate from '../../helpers/validate';

let PostForm = (props) => {
  const {handleSubmit} = props;
  const submit = (values) =>
  {
    // const {user, company, updateCompanyUser, getCompanyUsersList} = props
    // const company_id = company.get('id')
    // const token = user.auth_token
    // updateCompanyUser(company_id,values.id,token,values).then(res =>
    //   getCompanyUsersList(company_id, token)
    // )
    // props.history.push("/company/users")
  }

  //const title = (props.isNew ? 'Create' : 'Edit') + ' User'

  const title = 'form edit'
  return (
    <div>
      <h1>{title}</h1>
      <Form onSubmit={handleSubmit(submit)} className="edit_company_user" >
        <Field component="input" type="text" placeholder="Title" label="title" name="title" required/>
        <Field
          name="id"
          component="input"
          type="text"
          placeholder="id"
        />
        <Button type="submit">{title}</Button>
      </Form>
    </div>
  )
}

PostForm = reduxForm({
  form: 'post'
})(PostForm);

export default withRouter(PostForm)