import React from 'react'
import {FormControl, FormGroup, ControlLabel, HelpBlock} from 'react-bootstrap';

export function FieldGroup({id, label, help, ...props}) {
  // debugger
  return (
    <FormGroup controlId={id}>
      <ControlLabel>{label}</ControlLabel>
      <FormControl
        {...props}
        label={props.label}
        error={!!props.error}
        value={props.value}
        defaultValue={props.input.value}
        required={props.required}
        placeholder={props.placeholder}
        onChange={(param, data) => props.onChange(data.value)}
        type={props.type}
        //value={props.input.value}
      />
      {help && <HelpBlock>{help}</HelpBlock>}
    </FormGroup>
  );
}


//
// <SemanticInput {...input} type={type} label={label} placeholder={placeholder} required/>
//
// import React from 'react'
// import { Form } from 'semantic-ui-react';
//
// const SemanticInput = (props) => {
//   return (
//     <Form.Input
//       label={props.label}
//       error={!!props.error}
//       value={props.value}
//       defaultValue={props.defaultValue}
//       onChange={(param, data) => props.onChange(data.value)}
//       required={props.required}
//       placeholder={props.placeholder}
//       type={props.type}
//     />
//   )
// }
//
// export default SemanticInput
